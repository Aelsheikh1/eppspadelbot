# Disable HTTPS for local development
HTTPS=false
# SSL_CRT_FILE=localhost.pem
# SSL_KEY_FILE=localhost-key.pem

# Disable browser opening automatically
BROWSER=none

# Make the server listen on all interfaces (accessible from mobile)
HOST=0.0.0.0
